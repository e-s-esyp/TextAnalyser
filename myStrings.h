#ifndef ANALIZER_MYSTRINGS_H
#define ANALIZER_MYSTRINGS_H
namespace myStrings {
    bool isEqual(char *a, const char *b);

    bool endsWith(char *name, const char *filter);
}

#endif //ANALIZER_MYSTRINGS_H
